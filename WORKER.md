# Worker: pool-d

## Model: Opus

## Task: Types-first HARD decomp — JostleModule + ModelModule + TeamModule functions

Decomp HARD-tier functions that access these modules. All structs are recovered.

### Output: src/app/fun_typed_d_013.cpp onward (continue numbering)

### Rules
- Use struct field access, no raw offsets, no Ghidra paste, no naked asm
- 3-attempt limit per function
- ONLY create src/app/fun_typed_d_*.cpp
